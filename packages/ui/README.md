# @desiauction/ui

FLOODLIGHT component library — **empty shell until IP-1 opens** (IP-0_DESIGN §6). Will depend on React and design tokens only; never on `@desiauction/core`. Surfaces consume this package exclusively; apps never define visual primitives locally.
