node_modules/
.env
.env.*
!.env.example
.DS_Store
dist/
.next/
.next-e2e/

# Regenerated by every `next dev` / `next build`, with a `routes.d.ts` reference
# derived from distDir — which this app deliberately splits (.next vs .next-e2e),
# so a tracked copy is permanently dirty. The stable directives are committed in
# apps/web/next-globals.d.ts instead.
apps/web/next-env.d.ts

# Local media storage root (storage-port LOCAL_ROOT) — uploaded files, not source.
apps/web/public/_media/
coverage/
.turbo/
playwright-report/
test-results/
*.log
*.tsbuildinfo
.local/
