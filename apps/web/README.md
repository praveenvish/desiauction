# @desiauction/web

Next.js app — Console, Stage, Owner Room and registration surfaces (C-12), deployed on Vercel (`bom1`). IP-0 ships the shell: fail-closed env, security headers, `/healthz`, and the foundation page the E2E smoke targets. Real surfaces begin in IP-3 on the IP-1 design system.
