/**
 * Shared between the page and its loading shell. The skeleton renders the head
 * verbatim so the only thing that changes when the rows land is the rows — one
 * character of drift here (a straight quote against a curly one) is a visible
 * reflow at the moment the page settles.
 */
export const DIRECTORY_DESCRIPTION =
  "Every tournament on DesiAuction that's open to the public. Find yours, follow the live auction, and register as a player.";

export const DIRECTORY_KICKER = "Find a tournament";
